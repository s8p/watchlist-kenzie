import styled from "@emotion/styled";

export const Container = styled.div`
  background: var(--darkGray);
  height: 380px;
  display: flex;
  overflow-x: scroll;
  gap: 10px;
  align-items: center;
`;
